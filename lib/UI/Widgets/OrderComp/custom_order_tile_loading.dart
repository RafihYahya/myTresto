import 'package:flutter/material.dart';

class CustomOrderTileLoading extends StatelessWidget {
  const CustomOrderTileLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
class AppSettingsProvider {

  AppSettingsProvider();
  //implement your function that interface with the api here , it should return raw data 
  //the api will most likely be a local storage api to store settings data locally 
}